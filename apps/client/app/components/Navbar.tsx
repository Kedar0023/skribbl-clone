import { useState } from "react";
import useGameStore from "../store/gameStore";
import { PaperButton } from "./Button";
export default function Navbar() { const { round, totalRounds, wordToGuess, wordHint, timeInSec, roomId, isDrawer } = useGameStore(); const [copied, setCopied] = useState(false); const displayWord = isDrawer ? wordToGuess : wordHint || (wordToGuess ? wordToGuess.replace(/[a-zA-Z0-9]/g, "_ ") : ""); const copy = async () => { if (roomId) { await navigator.clipboard.writeText(roomId); setCopied(true); setTimeout(() => setCopied(false), 2000); } }; return <header className="relative z-20 flex items-center justify-between gap-3 border-b-2 border-paper-black bg-paper-white px-3 py-2 text-paper-black"><div className="rounded-paper border-2 border-paper-black bg-paper-lilac px-3 py-1 shadow-paper-sm"><p className="font-hand text-xl leading-none">Round {round}/{totalRounds}</p></div><div className="min-w-0 flex-1 text-center"><p className="font-body text-[10px] font-extrabold uppercase tracking-[.16em] text-paper-grey">{isDrawer ? "Your masterpiece" : "Guess the doodle"}</p><p className="truncate font-hand text-2xl tracking-[.12em]">{displayWord || "..."}</p></div><div className="flex items-center gap-2"><div className={`rounded-paper border-2 border-paper-black px-3 py-1 font-hand text-xl ${timeInSec <= 10 ? "bg-paper-pink animate-pulse" : "bg-paper-blue"}`}>{Math.floor(timeInSec / 60)}:{String(timeInSec % 60).padStart(2, "0")}</div>{roomId && <PaperButton variant="ghost" className="hidden px-3 py-1 text-base md:inline-flex" onClick={copy}>{copied ? "Copied!" : `Code: ${roomId}`}</PaperButton>}</div></header>; }
