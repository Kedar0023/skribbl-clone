import { useNavigate } from "react-router";
import useGameStore from "../store/gameStore";
import { PaperButton } from "./Button";
export default function Leaderboard() { const { users, currentUser, isHost, actions } = useGameStore(); const navigate = useNavigate(); const sorted = [...users].sort((a, b) => b.score - a.score); return <div className="absolute inset-0 z-50 flex items-center justify-center bg-paper-black/70 p-4"><section className="paper-surface paper-enter w-full max-w-lg p-6 text-center sm:p-8"><span className="inline-block rounded-full border-2 border-paper-black bg-paper-pink px-4 py-1 font-hand text-xl shadow-paper-sm">Game over!</span><h2 className="mt-4 font-hand text-5xl">Final standings</h2><p className="mb-5 font-body font-bold text-paper-grey">The doodle masters have been decided.</p><ol className="custom-scrollbar mb-6 max-h-64 space-y-2 overflow-y-auto text-left">{sorted.map((user, index) => <li key={user.id} className={`flex items-center justify-between rounded-paper border-2 border-paper-black p-3 font-body font-extrabold ${user.id === currentUser?.id ? "bg-paper-lilac" : "bg-paper-grey50"}`}><span>{index + 1}. {user.name} {user.id === currentUser?.id && "(You)"}</span><span className="font-hand text-2xl">{user.score} pts</span></li>)}</ol><div className="flex flex-wrap justify-center gap-3">{isHost ? <PaperButton onClick={() => actions.playAgain()}>↻ Play again</PaperButton> : <p className="self-center text-sm font-bold text-paper-grey">Waiting for the host…</p>}<PaperButton variant="ghost" onClick={() => { actions.leaveRoom(); navigate("/"); }}>← Leave room</PaperButton></div></section></div>; }
