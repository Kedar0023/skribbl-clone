import type { User } from "@repo/types/socket";
import useGameStore from "../store/gameStore";
export default function Players({ players, currentDrawerId }: { players: User[]; currentDrawerId: string | null }) { const { currentUser, hostId } = useGameStore(); const sorted = [...players].sort((a, b) => b.score - a.score); return <aside className="w-48 shrink-0 border-r-2 border-paper-black bg-paper-grey50 p-3 sm:w-60"><h3 className="mb-3 border-b-2 border-paper-black pb-2 font-hand text-2xl">Players <span className="text-lg">({players.length})</span></h3><ul className="custom-scrollbar space-y-3 overflow-y-auto">{sorted.map((player, index) => { const me = player.id === currentUser?.id; const drawing = player.id === currentDrawerId; return <li key={player.id} className={`rounded-paper border-2 border-paper-black p-2 shadow-paper-sm ${me ? "bg-paper-pink/40" : "bg-white"}`}><div className="flex items-center justify-between gap-1"><span className="truncate font-body text-sm font-extrabold">{index < 3 ? ["🥇", "🥈", "🥉"][index] : `${index + 1}.`} {player.name}</span><span className="font-hand text-lg">{player.score}</span></div><p className="mt-1 font-body text-[10px] font-extrabold uppercase tracking-wide">{me && "You "}{player.id === hostId && "♛ host "}{drawing && "✏ drawing"}</p></li>; })}</ul></aside>; }
