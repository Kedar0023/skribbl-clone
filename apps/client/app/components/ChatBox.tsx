import { useEffect, useRef, useState } from "react";
import useGameStore from "../store/gameStore";
import { PaperButton } from "./Button";
export default function ChatBox() { const { chatMessages, isDrawer, actions: { sendChat } } = useGameStore(); const [message, setMessage] = useState(""); const end = useRef<HTMLDivElement>(null); useEffect(() => { end.current?.scrollIntoView({ behavior: "smooth" }); }, [chatMessages]); const submit = (e: React.FormEvent) => { e.preventDefault(); if (!message.trim()) return; sendChat(message.trim()); setMessage(""); }; return <aside className="flex w-64 shrink-0 flex-col border-l-2 border-paper-black bg-paper-blue/25 sm:w-80"><header className="flex items-center justify-between border-b-2 border-paper-black p-3"><h3 className="font-hand text-2xl">Chat & guesses</h3><span className="rounded-full border-2 border-paper-black bg-white px-2 font-body text-xs font-extrabold">{chatMessages.length}</span></header><div className="custom-scrollbar flex-1 space-y-2 overflow-y-auto p-3">{chatMessages.length === 0 ? <p className="mt-6 text-center text-sm font-bold text-paper-grey">Your guesses will live here.</p> : chatMessages.map((msg) => <div key={msg.id} className={`rounded-paper border-2 border-paper-black p-2 text-sm ${msg.isSystem || msg.isCorrectGuess ? "bg-paper-pink/35 text-center font-extrabold" : "bg-white"}`}><span className="font-extrabold">{!msg.isSystem && `${msg.sender}: `}</span>{msg.message}</div>)}<div ref={end} /></div><form onSubmit={submit} className="flex gap-2 border-t-2 border-paper-black bg-white p-3"><input className="paper-input min-w-0 flex-1 px-3 py-2 text-sm" value={message} onChange={(e) => setMessage(e.target.value)} disabled={isDrawer} placeholder={isDrawer ? "You are drawing..." : "Type a guess"} /><PaperButton type="submit" className="px-3 py-1.5" disabled={isDrawer || !message.trim()}>Send</PaperButton></form></aside>; }
